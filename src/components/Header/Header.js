import React from "react";
import "./Header.css";

const Header = props => (
    <div className="header">
        <h1>Kirby Memory Game</h1>
        <h5>Select an image to earn points, but don't select the same one twice!</h5>
    </div>

)

export default Header;